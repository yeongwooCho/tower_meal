import 'package:flutter/material.dart';

class OfflineConsultingScreen extends StatelessWidget {
  static String get routeName => 'asdf';

  const OfflineConsultingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const Placeholder(),
      ],
    );
  }
}
